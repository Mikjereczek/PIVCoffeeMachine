<Window x:Class="CoffeeMachine.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CoffeeMachine"
        mc:Ignorable="d"
        Title="Automat do kawy" Height="450" Width="800">

    <Grid Margin="15">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="☕ Automatyczny ekspres do kawy"
                   FontSize="24"
                   FontWeight="Bold"
                   Margin="0,0,0,15"/>

        <StackPanel Grid.Row="1">

            <TextBlock Text="{Binding Status}"
                       FontSize="16"
                       Margin="0,0,0,10"/>

            <ProgressBar Height="20"
                         IsIndeterminate="{Binding IsBrewing}"
                         Margin="0,0,0,15"/>

            <GroupBox Header="Stan ekspresu">
                <StackPanel Margin="10">

                    <TextBlock Text="{Binding Water,
                        StringFormat=Woda: {0} ml}"/>

                    <TextBlock Text="{Binding CoffeeBeans,
                        StringFormat=Kawa: {0} g}"/>

                    <TextBlock Text="{Binding Milk,
                        StringFormat=Mleko: {0} ml}"/>

                </StackPanel>
            </GroupBox>

        </StackPanel>

        <StackPanel Grid.Row="2"
                    Margin="0,15,0,15">

            <Button Content="Espresso"
                    Command="{Binding EspressoCommand}"/>

            <Button Content="Cappuccino"
                    Command="{Binding CappuccinoCommand}"/>

            <Button Content="Latte"
                    Command="{Binding LatteCommand}"/>

            <Button Content="Uzupełnij zbiorniki"
                    Command="{Binding RefillCommand}"/>
        </StackPanel>

        <GroupBox Grid.Row="3"
                  Header="Historia zamówień">

            <ListBox ItemsSource="{Binding Orders}">
                <ListBox.ItemTemplate>

                    <DataTemplate>

                        <StackPanel Orientation="Horizontal">

                            <TextBlock Text="{Binding CoffeeName}"/>

                            <TextBlock Text=" - "/>

                            <TextBlock Text="{Binding OrderTime}"/>

                        </StackPanel>

                    </DataTemplate>

                </ListBox.ItemTemplate>
            </ListBox>

        </GroupBox>

    </Grid>
</Window>
